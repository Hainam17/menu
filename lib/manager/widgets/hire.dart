import 'package:flutter/material.dart';
class Hire extends StatelessWidget {
  const Hire({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Center(
      child: Text(
        'Không có dữ liệu người ứng tuyển!'
      ),
    );
  }
}
